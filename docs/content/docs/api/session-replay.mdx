---
title: Session Replay
description: |
  Ingest session replay events (DOM mutations, clicks, scrolls, etc.).
  Supports batching with no enforced limit. Events within a batch should
  be sorted by `timestampMillis` ascending.
full: true
_openapi:
  method: POST
  toc:
    - depth: 2
      title: Ingest a batch of session replay events
      url: '#ingest-a-batch-of-session-replay-events'
  structuredData:
    headings:
      - content: Ingest a batch of session replay events
        id: ingest-a-batch-of-session-replay-events
    contents:
      - content: >
          Submits session replay events (DOM mutations, clicks, scrolls,

          network events, console messages, etc.) for a given session.


          - `fingerprint` is used to maintain event ordering within a session

          - Use the same `fingerprint` for all batches within a session

          - Events should be sorted by `timestampMillis` ascending

          - `eventData` must be a JSON-serialized **string**, not a nested
          object

          - No enforced batch size limit (recommended: under 200 events /
          ~500KB)


          **Privacy:** The SDK must mask sensitive data before sending:

          - Replace password field values with `"[MASKED]"`

          - Strip `Authorization`, `Cookie`, `X-SeeStack-Key` headers

          - Strip sensitive query parameters from URLs
        heading: ingest-a-batch-of-session-replay-events
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

<APIPage document={"./openapi-sdk.json"} webhooks={[]} operations={[{"path":"/ingest/v1/replay","method":"post"}]} showTitle />