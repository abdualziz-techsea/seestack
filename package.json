{
  "name": "seestack",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @seestack/web dev",
    "build": "pnpm --filter @seestack/shared build && pnpm --filter @seestack/web build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
